[package]
name = "structural-analysis"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
description = "2D structural analysis: beam and frame FEM, solver selection, mechanism diagnostics."
license = "MIT"
repository = "https://github.com/yandongqiang/section-properties"
keywords = ["structural", "analysis", "fem", "beam", "frame"]
categories = ["science", "mathematics"]

[dependencies]
section-properties = { path = "../section-properties" }
thiserror = "1.0"

[features]
pardiso = ["section-properties/pardiso"]
